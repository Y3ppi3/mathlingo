# mathlingo-frontend/.env
#
# VITE_API_URL намеренно не задан: фронт сам выводит адрес бэкенда из хоста
# страницы (localhost и LAN-IP с телефона — оба работают без правок).
# Раскомментируйте, только если бэкенд на другом хосте/порту.
# VITE_API_URL=http://localhost:8000
